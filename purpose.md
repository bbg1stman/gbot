# 목적

gbot는 고졸 검정고시 수험생의 **현재 수준을 진단하고**, 그 결과에 맞춰 **무엇을 어떤 순서로 할지 계획**하는 앱이다.

위키를 읽으며 헤매는 학습이 아니다. 수험생은 위키를 돌아다니지 않는다.

## 앱 본체

앱이 읽는 런타임은 `pack/` 이다. 편집 소스는 `data/` 다.

1. **`data/bank/`** — 시행·과목·문항 id를 고정한다. 공식 기출은 슬롯만 둔다.
2. **`data/diagnostics/`** — 과락·합격선에 맞춘 밴드와 과목별 진단 설계.
3. **`data/plans/`** — 밴드별 주간 템플릿 (개념/유형/기출슬롯 포함 여부).
4. **`data/app/`** — 사용자·시도·세션·계획·오답노트·유형통계 기록. 실명·연락처는 두지 않는다.
5. **`pack/`** — 위 소스와 위키 개념을 컴파일한 앱용 테이블. Android/web은 여기만 읽는다.

## 위키는 교무실

`wiki/` 는 개념·유형·교육과정 그래프다. 문항 저장소가 아니고 수험생 UI가 아니다.
교사가 카드를 쓰거나 에이전트가 단원 이름을 확인할 때만 본다.

## 순서

1. **은행 id를 먼저 고정한다.** 진단 결과를 같은 자리에 다시 붙일 수 있어야 한다.
2. **공식 기출은 슬롯으로만 둔다.** 라이선스 전에는 원문이 없어도 출제 범위·문항 수·과목 비중을 알 수 있다.
3. **진단은 블루프린트의 축으로 뽑는다.** 지문이 없는 슬롯은 출제하지 않고 계획·통계에만 쓸 수 있다.
4. **계획 템플릿은 밴드를 키로 쓴다.** 미달이면 개념+유형, 경계면 유형+기출슬롯.
5. **라이선스 후 같은 id로 official 원문을 ingest** 한다. 진단 로그는 id를 바꾸지 않는다.
6. **빈칸은 새 문항으로 메운다.** `source=original`. 기출을 베끼지 않는다.

## 왜 슬롯이 필요한가

공식 기출은 시도교육청 저작물이다. 원문 없이 id를 먼저 심어 두면,

- 저작권을 어기지 않고 앱을 돌릴 수 있고
- 나중에 원문이 들어와도 학습 이력이 깨지지 않는다.

원문이 생기기 전에 기억이나 PDF로 슬롯을 채우지 않는다.
